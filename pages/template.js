import React, { memo } from "react";

import AppLayout from "../components/src/AppLayout";

const Template = memo(() => {
  return (
    <AppLayout>
      <div>template</div>
    </AppLayout>
  );
});

export default Template;
